$("#tag_item").append("<%= escape_javascript(render partial: 'tag', collection: @post, as: :post) %>");
$("#tag_more_link").replaceWith("<%= escape_javascript(link_to_next_page(@post, 'つづきを見る', remote: true, id: 'tag_more_link', class: 'btn btn-info btn-block', data: { disable_with: "読み込み中 <i class='fa fa-spinner fa-spin'></i>" })) %>");