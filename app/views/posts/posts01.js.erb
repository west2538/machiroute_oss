$("#items01").append("<%= escape_javascript(render partial: 'posts01', collection: @posts01, as: :post) %>");
$("#more_link01").replaceWith("<%= escape_javascript(link_to_next_page(@posts01, 'つづきを見る', remote: true, id: 'more_link01', class: 'btn btn-info btn-block', params: { type: :posts01 }, data: { disable_with: "読み込み中 <i class='fa fa-spinner fa-spin'></i>" })) %>");